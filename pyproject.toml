[project]
name = "duet-of-models"
version = "0.1.0"
description = "Workspace metadata for duet-of-models. Use envs/* for runnable dependency sets."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []
